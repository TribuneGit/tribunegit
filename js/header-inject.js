/**
 * Injects the shared wizard Alpine wrapper, header, and mobile nav
 * Used on service pages, about, pricing, etc.
 * Call: injectHeader() after DOM ready
 */

// All service pages initialize wizard the same way
// Alpine.js picks up x-data="wizardData()" on data-wizard-root
